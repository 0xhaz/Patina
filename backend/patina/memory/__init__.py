"""Patina memory engine — the differentiator.

Multi-scoped store (format / exception / entity / episodic), hierarchical
distillation, decay + hard-invalidation, and hybrid retrieval (semantic +
structured + decay) in a single SQL query over Postgres + pgvector.
"""
